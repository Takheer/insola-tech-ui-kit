export type TSelectInputProps = {
  label?: string,
  options: any[],
  itemValue?: string,
  itemLabel?: string,
  error?: boolean,
  errorMessage?: string
  placeholder?: string
  dense?: boolean
  selectedItemId?: string
};